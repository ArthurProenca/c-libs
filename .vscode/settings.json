{
    "editor.tabSize": 4,
    "editor.insertSpaces": true,  // Ensures spaces instead of tab characters
    "editor.detectIndentation": false // Prevents VS Code from auto-detecting different indentation styles
}
